development:
  rails:
    servers:
      - server:
          same_as: frontends
  mysql:
    servers:
      - server:
          unique_name: frontends
          vendor: aws
          region: us-west-1
          size: t1.micro       
staging:
  rails:
    servers:
      - server:
          unique_name: frontend
  mysql:
    servers:
      - server:
          same_as: frontend
production:
  rails:
    servers:
      - server:
          unique_name: frontends
          address: 123.123.123.123
          username: fred
          ssh_key_name: test
  redis:
    servers:
      - server: external